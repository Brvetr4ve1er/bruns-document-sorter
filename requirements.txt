# BRUNs - runtime dependencies (UTF-8)
#
# Pin policy:
#   * Use compatible-release operator (~=) so pip can resolve a wheel for
#     the actual Python version on the target machine. Hard pins (==) caused
#     install failures on fresh PCs because some pinned versions only ship
#     wheels for very recent Python (e.g. pandas 3.0 has no cp310 wheel).
#   * If a version is known-bad, pin against it explicitly with !=
#
# Minimum supported Python: 3.11 (pandas 3.x dropped 3.10 wheels)
#   The installer (INSTALL.bat / install.sh) enforces this.
#
# To regenerate after adding/removing imports:
#     python tests/_third_party_scan.py

# --- Data extraction & OCR ---------------------------------------------
PyMuPDF~=1.27.0
pytesseract~=0.3.13
Pillow~=12.2.0
passporteye~=2.2.0
mrz~=0.6.0

# --- LLM client ---------------------------------------------------------
requests~=2.33.0

# --- Schemas & validation ----------------------------------------------
pydantic~=2.13.0

# --- Storage & search --------------------------------------------------
chromadb~=1.5.0
pandas>=2.2,<4         # 2.2 wheels for py3.10–3.13; 3.x for py3.11+
openpyxl~=3.1.0

# --- Pipeline & matching -----------------------------------------------
huey~=3.0.0
RapidFuzz~=3.14.0

# --- BI bridge ---------------------------------------------------------
Flask~=3.1.0
flask-cors~=5.0.0

# --- Dev / test --------------------------------------------------------
pytest~=9.0.0
